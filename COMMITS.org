#+TITLE: Commit Log
#+STARTUP: overview

* 001

#+begin_example
001 chore: initialize runner repository with CLI skeleton
#+end_example

Established:

- =run.py=
- =README.org=
- =.gitignore=
- =tests/=
- CLI arguments =--dry-run=, =--run=, =--verbose=
- positional manifest path

* 002

#+begin_example
002 feat: define manifest schema and authority zones
#+end_example

Added the canonical manifest contract:

#+begin_example
step,mode,input,output,authority_zone
#+end_example

Added allowed modes:

- =load_csv=
- =run_sql=
- =export_sql=

Added authority zones:

- =source=
- =prepared=
- =review=
- =focused=
- =inform=
- =business=
- =report=

The contract is centralized in =manifest_schema.py=.


* 003

#+begin_example
003 feat: implement dry-run manifest validation
#+end_example

Added pre-execution validation for:

- manifest existence and readability
- exact header structure
- positive integer step values
- duplicate steps
- allowed modes
- allowed authority zones
- required inputs
- mode-dependent outputs

Added structured =ValidationIssue= records and basic unit-test fixtures.


* 004

#+begin_example
004 feat: add human-readable validation reporting
#+end_example

Added a dedicated reporting layer for dry-run validation.

The reporter:

- describes success as "ready to run"
- describes failure as "not ready to run"
- renders each problem as prose
- gives an explicit corrective hint
- confirms that no package steps were executed after validation failure
- shows internal validation types only in verbose mode

This separates validation logic from presentation logic.


* 005

#+begin_example
005 feat: add verbose dry-run reporting
#+end_example

Added verbose inspection of structurally valid manifests.

Verbose dry run now reports:

- manifest line number
- step
- mode
- authority zone
- input
- output
- resolved relative paths

Relative paths are resolved from the manifest directory.
Inspection remains separate from validation and execution.


* 006

#+begin_example
006 feat: add run execution skeleton
#+end_example

Added the first =--run= execution path.

The runner now:

- validates before execution
- refuses to build an execution plan when validation fails
- walks valid manifest rows in step order
- emits one pending execution result per manifest row
- performs no data movement yet

This establishes the execution lifecycle before concrete mode handlers are
introduced.


* 007

#+begin_example
007 feat: implement load_csv execution
#+end_example

Added the first concrete manifest mode.

=load_csv= now:

- resolves the source CSV relative to the manifest
- recreates the package DuckDB database for each run
- creates or replaces the declared target relation
- loads CSV data with DuckDB =read_csv_auto=
- reports the loaded row count

Added =duckdb= to =requirements.txt= and added CSV execution fixtures/tests.


* 008

#+begin_example
008 feat: implement run_sql execution
#+end_example

Added the second concrete manifest mode.

=run_sql= now:

- resolves SQL files relative to the manifest
- reads UTF-8 SQL text
- rejects empty SQL files
- executes SQL against the current DuckDB connection
- allows later SQL layers to depend on relations created by earlier steps

The SQL file owns the names of any views or tables it creates.

Added a Prepared-layer SQL fixture and tests.


* 009

#+begin_example
009 feat: implement export_sql execution
#+end_example

Added the third concrete manifest mode.

=export_sql= now:

- accepts a relation name or SELECT/WITH query
- resolves output paths relative to the manifest
- creates parent output directories
- exports CSV with a header
- reports the exported row count

The sample manifest now runs the complete path from CSV source through SQL
transformation to CSV export.

All three manifest execution modes are now implemented.


* 010

#+begin_example
010 feat: add execution error handling and prose diagnostics
#+end_example

Added structured runtime error handling.

The runner now:

- stops at the first execution failure
- preserves already completed step results
- skips all later steps
- maps common exceptions into stable problem types
- gives a human-readable corrective hint
- keeps verbose internal error categories optional

Added a deliberately failing SQL package fixture for negative execution
coverage.


* 011

#+begin_example
011 test: add product bundle example package integration coverage
#+end_example

Added a complete end-to-end example package under
=tests/resources/product_bundle_example=.

The fixture includes:

- Product
- Attribute
- Product Attribute
- Bundle
- Product Bundle
- all seven authority zones
- a bundle-grain wide Review layer
- Focused and Inform derivations
- Business and stable Report views
- an expected exported report
- negative manifests for structural and runtime failures

The package now serves as documentation, integration coverage, and a
regression fixture.


* 012

#+begin_example
012 docs: document package contract and command-line interface
#+end_example

Added dedicated Org-mode documentation for:

- the package and manifest contract
- authority-zone semantics
- path resolution
- validation behavior
- fresh-run execution
- runtime failure behavior
- command-line usage
- the Product / Attribute / Bundle example package

This completes the original twelve-commit runner repository plan.


* 013

#+begin_example
013 feat: add best-effort relation dependency discovery
#+end_example

Begins development cycle 2.

Verbose dry run now discovers, where practical:

- relations created by =load_csv=
- relations created by =run_sql=
- relations referenced by =run_sql=
- relations referenced by =export_sql=

The dependency view is descriptive only in this commit. Unresolved
dependency validation is intentionally deferred to commit 014.


* 014

#+begin_example
014 feat: validate relation dependencies before execution
#+end_example

Added dependency validation on top of v013 discovery.

The runner now:

- tracks relations created by earlier manifest steps
- reports references that are unresolved at the point of use
- blocks =--run= before execution when dependency validation fails
- renders dependency failures as prose with corrective hints
- preserves verbose dependency inspection for diagnosis

Added an unresolved-relation negative fixture and tests.


* 015

#+begin_example
015 feat: validate authority-zone dependency progression
#+end_example

Added dependency-aware authority-zone checks.

The runner now rejects a relation dependency when its producer belongs to a
later authority zone than its consumer.

Same-zone and earlier-zone dependencies remain valid.

The check is based on actual relation dependencies rather than global
manifest ordering, allowing independent analytical branches while preventing
backward authority flow.


* 016

#+begin_example
016 feat: add package summary to verbose dry run
#+end_example

Added a package-level summary after successful verbose validation.

The summary includes:

- step count
- mode counts
- authority-zone counts
- created relations
- terminal created relations
- export outputs

This gives dry run a concise package-level view in addition to step-level
inspection and dependency details.


* 017

#+begin_example
017 feat: add analytical grain declarations and dry-run reporting
#+end_example

Added optional package-level grain declarations at
=specifications/grain.csv=.

Verbose dry run can now show:

- declared grain for relations
- composite grain keys
- grain-preserving dependencies
- grain-changing dependencies

The Product / Attribute / Bundle example declares source and analytical
grains, making the aggregation from =bundle_id + product_id= to =bundle_id=
visible before execution.


* 018

#+begin_example
018 feat: add optional relation contracts for columns keys and types
#+end_example

Added optional relation contracts at
=specifications/relation_contract.csv=.

Contracts may declare:

- expected columns
- expected DuckDB data types
- simple or composite key columns

Static validation connects contracts with package-created relations and
grain declarations. A runtime validator is included for realized schema and
key uniqueness checks.


* 019

#+begin_example
019 feat: add contextual SQL execution diagnostics
#+end_example

Improved runtime SQL failure reporting with:

- resolved SQL file path
- extracted SQL line number when DuckDB reports one
- source text for the reported line
- created-relation context
- referenced-relation context

Verbose diagnostics expose dependency context while the normal report stays
focused on the corrective location.


* 020

#+begin_example
020 feat: add deterministic artifact checks and readable csv diffs
#+end_example

Added optional =specifications/artifacts.csv= declarations, SHA-256
deterministic comparison, readable CSV row differences, and non-zero status
for artifact mismatches.


* 021

#+begin_example
021 feat: add machine-readable json dry-run output
#+end_example

Added a versioned JSON dry-run contract. Base output contains readiness and
categorized issues; verbose output adds the package analysis model.


* 022

#+begin_example
022 feat: add package-level test command
#+end_example

Added =--test= as a first-class package operation.

The test lifecycle combines preflight validation, fresh execution, runtime
relation-contract checks, and deterministic artifact comparison into one
pass/fail command suitable for local development and CI.


* 023

#+begin_example
023 feat: add review analytical-base export to product bundle example
#+end_example

Kept the furniture example in the runner package and added a deterministic
Review-layer analytical-base export alongside the final Report export.


* 024

#+begin_example
024 docs: freeze cycle 2 analysis contract
#+end_example

Closed Cycle 2 by documenting the complete package analysis contract,
authority-zone semantics, dry-run and package-test interfaces, deterministic
artifact contract, and the product-bundle reference example.

Added contract-surface regression tests and a package review checklist.
